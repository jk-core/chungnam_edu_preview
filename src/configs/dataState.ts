/**
 * RTU 가 실어 온 수집 데이터 자체의 상태 (BE 코드 테이블).
 *
 * NAME 은 목업이 쓰는 자리다. 실제 응답은 dataStateCode 와 dataStateName 을 짝으로 내려주므로
 * 화면은 내려온 이름을 그대로 적고, 여기 CODE 는 「정상인가」를 가릴 때만 본다.
 */
export const DATA_STATE = {
  CODE: {
    정상: 28001,
    타임아웃: 28002,
    프로토콜에러: 28003,
    인버터고장: 28004,
    발전량임계치초과: 28005,
    누적값감소: 28006,
    누적값영: 28007,
    누적값음수: 28008,
    누적값초과: 28009,
    누적값없음: 28010,
  },
  NAME: {
    28001: '정상',
    28002: 'TIME-OUT',
    28003: '프로토콜에러',
    28004: '인버터 고장',
    28005: '발전량 임계치 초과',
    28006: '누적값 감소',
    28007: '누적값 0',
    28008: '누적값 음수',
    28009: '누적값초과',
    28010: '누적값 없음(NULL)',
  },
} as const;

export const DATA_STATE_CODES = Object.values(DATA_STATE.CODE);
